This project defines a simple empty class Square in Python.
# Python - Classes

Task 1 defines a class `Square` with a private attribute `__size`.
### 2. Size validation
- File: `2-square.py`
- Defines a class `Square` that validates the `size` value:
  - `size` must be an integer.
  - `size` must be >= 0.
  - Raises `TypeError` or `ValueError` when conditions are not met.
